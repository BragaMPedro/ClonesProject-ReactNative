import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
   carrosselContainer: {
      height: 106,
      width: "100%",
      borderRadius: 10,
   },

   img: {
      width: 330,
      height: 106,
      borderRadius: 8,
   },

   pageMarkerContainer: {
      flexDirection: "row",
      alignItems: "center",
      alignContent: "center",
      marginVertical: 10,
   }
})
